# Ephemeris Layer

JavaScript ephemeris calculations used by the converted almanac renderers.

The module exposes nautical angle formatting, Sun/Moon rows, planet rows, star rows, rise/set events, meridian passage, equation-of-time values, moon age, and illumination. Runtime calculations are JavaScript-only and use `astronomy-engine` plus local formatting logic.
